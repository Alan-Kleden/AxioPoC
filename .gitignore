# --- START .gitignore (Optimized Final PoC) ---

# 1) Artefacts lourds / binaires de données (toujours hors repo)
*.parquet
*.pkl
*.npz
*.npy
*_emb_*.parquet
artifacts_xfer/

# 2) Jeux de données & sorties massives (ignorer par défaut, sauf dossier final)
#   - on ignore des sous-rapports verbeux…
REPORTS/temporal_explore/*.csv
REPORTS/holdout_*/**
REPORTS/face_validity/**/top*_messages.csv
REPORTS/face_validity/**/bottom*_messages.csv
#   - on ignore les CSV génériques dans REPORTS…
REPORTS/*.csv
#   …mais on GARDE tout ce qui est dans le paquet final :
!REPORTS/final_poc_afD/**
#   (les règles d’exception doivent venir APRÈS les règles d’ignore)

# 3) Caches / venv / logs
.venv/
venv/
__pycache__/
*.log
*.tmp
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# 4) Archives locales & backups
*_archive*/
_quarantine*/
*.zip
*.bak

# 5) IDE / OS / secrets
.DS_Store
Thumbs.db
.vscode/
.idea/
*.env
.env*

# --- END .gitignore ---
